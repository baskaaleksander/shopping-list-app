# Localization Conventions

All user-facing copy must come from translation resources.

- Use `useTranslation()` inside components instead of hardcoded strings.
- Add new English and Polish keys together.
- Use English as the fallback for unsupported device locales.
